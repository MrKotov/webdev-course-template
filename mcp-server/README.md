# Exercise 1 · MCP server

Your hand-written MCP server lives in this folder.

## How to start it
<!-- The exact command, e.g. `node mcp-server/server.js` -->

## Registering it with your agent
- Gemini CLI: add it under `mcpServers` in `.gemini/settings.json`, or run `gemini mcp add <name> <command> [args...]`.
- Copilot (optional): add it under `servers` in `.vscode/mcp.json`. MCP tools appear only in Agent mode.

## Session transcript
<!-- Paste one full session: handshake, tools/list, one tools/call. -->

## Description experiment
<!-- Three sentences: you changed only the tool's description. Did the model choose it differently? -->
